[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prana"
version = "0.1.0"
description = "The runtime shell — autonomous heartbeat that drives Narada's cycle between human sessions"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [
    { name = "Sutirtha Basak", email = "sutirthabasak@gmail.com" },
    { name = "Narada", email = "narada@fractal.co.nz" },
]
keywords = ["agent", "heartbeat", "narada", "viveka", "ai-identity", "hermes"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: POSIX :: Linux",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: MacOS",
]
dependencies = [
    # Memory tree + MCP server
    "smriti @ git+https://github.com/Stormbane/smriti.git@master",
    # Body — display, expression, voice clients
    "deha @ git+https://github.com/Stormbane/deha.git@master",
    # Core
    "pyyaml>=6.0",
    "requests>=2.31",
    # Session manager (a default-enabled host component — required, not
    # optional; an extras-only dep would restart-loop clean installs)
    "psutil>=5.9",
    "mcp>=1.0",
    # Long-standing undeclared imports (chat bridge) + voice budget/gate
    "python-dotenv>=1.0",
    # <2: torch/scipy in this env are compiled against numpy 1.x; numpy 2
    # breaks them (librosa/audiomentations will try to pull it — don't let them)
    "numpy>=1.26,<2",
]

[project.optional-dependencies]
voice = [
    # Voice loop (component ships enabled:false, so extras are correct
    # here — flip requires deliberate setup incl. OPENAI_API_KEY)
    "livekit-agents[openai]>=1.6",
    "livekit-wakeword>=0.2",
    "pyaudio>=0.2.14",   # WakeWordListener mic capture (standalone path)
]
wakeword-train = [
    # Training the "Narada" wake model (scripts/train-wakeword.ps1).
    # livekit-wakeword lazily imports these across its generate/augment/
    # export stages without declaring them; pin them all so a clean
    # environment trains reproducibly. Also needs the espeak-ng BINARY
    # on PATH — the script installs it via MSI extract.
    "livekit-wakeword>=0.2",
    "webrtcvad-wheels>=2.0",  # generate: VAD silence trim (prebuilt wheels)
    "pronouncing>=0.2",       # generate: adversarial negative phrases
    "audiomentations>=0.43",  # augment: noise/reverb
    "librosa>=0.11",          # augment/features: audio feature extraction
    "onnx>=1.22",             # export: ONNX model emission
]
viveka = [
    # Local Qwen+LoRA inference for the viveka core
    "torch>=2.1",
    "transformers>=4.40",
    "peft>=0.13",
    "bitsandbytes>=0.43",
    "unsloth[cu121-torch240]",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.10",
]

[project.scripts]
prana-heartbeat = "prana.heartbeat.__main__:main"
prana-host = "prana.host.cli:main"

[project.urls]
Repository = "https://github.com/Stormbane/prana"
Issues = "https://github.com/Stormbane/prana/issues"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
